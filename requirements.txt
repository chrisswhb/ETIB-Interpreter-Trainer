# ETIB Arabic I'rab Detection System — Dependencies
# Python 3.10+

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # for UploadFile / Form
pydantic>=2.0.0

# ── HTTP client (Groq API calls) ───────────────────────────────────────────────
httpx>=0.27.0

# ── Audio processing ───────────────────────────────────────────────────────────
# ffmpeg must be installed separately (see README)
imageio-ffmpeg>=0.4.9        # Python-bundled ffmpeg fallback
soundfile>=0.12.1            # WAV I/O
scipy>=1.12.0                # fallback WAV reader

# ── ML / speech ───────────────────────────────────────────────────────────────
torch>=2.2.0                 # CPU is fine; GPU optional
torchaudio>=2.2.0            # audio utilities
transformers>=4.40.0         # wav2vec2 models
numpy>=1.26.0

# ── Silero VAD (loaded via torch.hub) ─────────────────────────────────────────
# No pip install needed — torch.hub.load("snakers4/silero-vad") fetches it

# ── Optional: better resampling ────────────────────────────────────────────────
librosa>=0.10.0

scikit-learn>=1.4.0
joblib>=1.3.0